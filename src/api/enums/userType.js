// Export an object with user types as properties
module.exports = Object.freeze({
    // Constant representing an admin user type
    ADMIN: 'ADMIN',
    // Constant representing a regular user type
    USER: 'USER'
});
